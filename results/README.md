# Training logs (GPU runs)

These `training.log` files were copied from a GPU machine for reference (ES / GRPO / ZO countdown experiments). Model weights are **not** stored in git (see root `.gitignore`).
